Both the Pi(server) and the Laptop(client) should be on the same network.
the proper IP address of the server to be changed in the client code.

In this I had the Raspberry Pi as the server and my laptop as the client so the IP in the client code is the Pi.

to get IP address do -> hostname -I 